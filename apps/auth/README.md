# 登录模块

## 开发

```sh
pnpm install
cd apps/auth
pnpm dev -- --port 4174
```

## 页面占位

- `/login`：账号登录入口
- `/register`：团队注册入口

## 样式

- Tailwind 配置位于 `tailwind.config.js`
- 全局样式在 `src/assets/main.scss` 中引入 `@tailwind` 指令
